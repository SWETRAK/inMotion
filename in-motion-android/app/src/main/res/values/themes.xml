<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Inmotionandroid" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/primary</item>
        <item name="android:background">@color/background</item>
        <item name="android:textColorHint">@color/white</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:colorButtonNormal">@color/primary</item>
        <item name="android:fontFamily">@font/quicksand</item>
        <item name="dividerColor">@color/primary</item>
    </style>
    <style name="FilledBoxEndIconTint" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="endIconTint">@color/white</item>
    </style>

    <style name="Theme.Inmotionandroid" parent="Base.Theme.Inmotionandroid" />
</resources>